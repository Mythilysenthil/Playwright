# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: WindowsAction.test.ts >> Iterate Window Attribute
- Location: Demo_project\tests\WindowsAction.test.ts:19:1

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.click: Test timeout of 30000ms exceeded.
Call log:
  - waiting for locator('#tabButton')

```

# Test source

```ts
  1  | import { test , expect } from '@playwright/test';
  2  | 
  3  | test("Iterate windows" ,async({page , context})=>{
  4  |     await page.goto("https://demoqa.com/browser-windows/");
  5  | 
  6  |     const [tab] = await Promise.all([context.waitForEvent("page"), page.locator('#tabButton').click()]);
  7  |     await tab.waitForLoadState("domcontentloaded");
  8  |     const [window] = await Promise.all([context.waitForEvent("page"), page.locator('#windowButton').click()]);
  9  |     await window.waitForLoadState("domcontentloaded");
  10 |     const pages = context.pages();
  11 |     console.log("Number of pages:" ,pages.length);
  12 |     for(const p of pages){
  13 |         console.log("------------------");
  14 |         console.log("URL:",p.url);
  15 |         console.log("Title:",await p.title());
  16 |     }
  17 | });
  18 | 
  19 | test("Iterate Window Attribute", async({page, context}) =>{
  20 | 
  21 |     const [newTabe] = await Promise.all([
  22 |         context.waitForEvent("page"),
> 23 |         page.locator("#tabButton").click()
     |                                    ^ Error: locator.click: Test timeout of 30000ms exceeded.
  24 |     ]);
  25 |     await newTabe.waitForLoadState("domcontentloaded")
  26 |     const pages = context.pages();
  27 |     console.log("Total Pages Opened: ${pages.length}");
  28 |     for(const p of pages){
  29 |         console.log("URL: ", p.url());
  30 |         if(p.url().includes("simple")){
  31 |             const text = await p.locator("simpleHeading").textContent();
  32 |             console.log("Simple Page heading: ", text)
  33 |         }
  34 |     }
  35 | });
```